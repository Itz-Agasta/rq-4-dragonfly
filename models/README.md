# models

Trained ONNX artifacts, committed on purpose. They are small and they are part of the runtime: `dragonfly-core` loads them through `ort`.

Every model here is produced by a script in `../ml/`, never edited by hand.
